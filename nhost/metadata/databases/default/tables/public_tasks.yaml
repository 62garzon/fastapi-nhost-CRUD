table:
  name: tasks
  schema: public
insert_permissions:
  - role: user
    permission:
      check: {}
      columns:
        - title
        - description
        - is_completed
        - updated_at
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - title
        - description
        - is_completed
        - created_at
        - updated_at
      filter: {}
      allow_aggregations: true
      query_root_fields:
        - select
        - select_by_pk
      subscription_root_fields:
        - select
        - select_by_pk
update_permissions:
  - role: user
    permission:
      columns:
        - id
        - title
        - description
        - is_completed
        - updated_at
      filter: {}
      check: null
delete_permissions:
  - role: user
    permission:
      filter: {}
